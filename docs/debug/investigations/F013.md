---
issue_id: F013
current_phase: 2
head_sha: e077cd62b564efecbe5064978dc3e38a04ba12d0
# fix-branch HEAD — unchanged (scratchpad-only phase 1 commit)
phases:
  1_root_cause_investigation:
    complete: true
    gate: "Root cause chain traced to a specific file:line. Evidence recorded in Phase 1 section. No fix proposed."
  2_pattern_analysis:
    complete: false
    gate: "C++ reference behaviour confirmed at ~/Projects/eaglemode-0.96.4/. Difference from Rust identified and recorded."
  3_hypothesis_and_testing:
    complete: false
    gate: "Root cause confirmed by a passing hypothesis. At least one hypothesis tested and recorded with CONFIRMED or RULED OUT outcome."
  4_implementation:
    complete: false
    gate: "Fix committed. cargo check, cargo clippy -- -D warnings, and cargo-nextest ntr all pass."
---

## Issue

F013 — Panic when zooming into file manager: `CreateControlPanel requires scheduler-reach PanelCtx`.
Panic site: `crates/emfileman/src/emDirPanel.rs:529` (expect on `parent_ctx.as_sched_ctx()`).
Triggered by zooming into the File System panel; surfaced after the F012 ContentControlPanel wiring landed on main (commit 851ca26b).

## Prior-session context

Session `cadaa424-95fd-48be-beee-2e8257ed1569` (F012 investigation, 2026-04-24) noted:
"ContentControlPanel lives in the control sub-tree but must be created based on the active panel in the content sub-tree (a separate tree). Cross-tree communication mechanism not yet defined."
The resulting F012 fix implemented `create_control_panel_in` cross-tree. That cross-tree dispatch is the suspected caller that now hands `emDirPanel::CreateControlPanel` a PanelCtx without scheduler reach.

## Next Steps

1. Read `crates/emfileman/src/emDirPanel.rs:520-540` to confirm panic site and what `as_sched_ctx()` requires.
2. Read `crates/emmain/src/emMainWindow.rs` ControlPanelBridge::Cycle to see how CreateControlPanel is dispatched cross-tree (commit 851ca26b).
3. Find the definition of `PanelCtx` / `as_sched_ctx` / `create_control_panel_in` to understand which PanelCtx flavours have scheduler reach.
4. Inspect C++ `emPanel::CreateControlPanel` callsite in emView/emMainWindow to compare what context C++ passes.

## Phase 1 — Root Cause Investigation

**E1.1** (`crates/emfileman/src/emDirPanel.rs:525-537`): `emDirPanel::CreateControlPanel`
calls `parent_ctx.as_sched_ctx().expect("CreateControlPanel requires scheduler-reach PanelCtx")`
and then constructs `emFileManControlPanel::new(&mut sched, ctx)`. The expect is the
observed panic site.

**E1.2** (`crates/emfileman/src/emFileManControlPanel.rs:92`):
`emFileManControlPanel::new` has signature
`pub fn new<C: emcore::emEngineCtx::ConstructCtx>(cc: &mut C, ctx: Rc<emContext>)`.
Requires a `ConstructCtx` (i.e. `SchedCtx`/`InitCtx`) to register engines and
signals for the new control-panel widgets. Scheduler reach is genuinely required
here — not a defensive check.

**E1.3** (`crates/emcore/src/emEngineCtx.rs:560-574`): `PanelCtx::as_sched_ctx`
returns `Some(SchedCtx)` only when all five of `scheduler`, `framework_actions`,
`root_context`, `framework_clipboard`, `pending_actions` are `Some`. Any `None`
returns `None`.

**E1.4** (`crates/emcore/src/emEngineCtx.rs:468-482`): `PanelCtx::new(tree, id, tallness)`
sets all five handles to `None`. Only `PanelCtx::with_sched_reach` (lines 532-553)
attaches them.

**E1.5** (`crates/emcore/src/emPanelTree.rs:2109-2132`): `create_control_panel_in`
(the cross-tree variant added by F012 fix 851ca26b) builds the PanelCtx via
`PanelCtx::new(target_tree, parent_arg, current_pixel_tallness)` — the no-reach
constructor. This PanelCtx is then passed to the inner `behavior.CreateControlPanel`
call, so `parent_ctx.as_sched_ctx()` returns `None` and E1.1's `.expect` panics.

**E1.6** (`crates/emmain/src/emMainWindow.rs:807-881`, `ControlPanelBridge::Cycle`):
The production call site holds `&mut EngineCtx<'_>` which carries all five
scheduler-reach handles (`ctx.scheduler`, `ctx.framework_actions`,
`ctx.root_context`, `ctx.framework_clipboard`, `ctx.pending_actions`). The fields
are independent of `ctx.windows` so they can be split-borrowed alongside `win`.
Nothing in the current call site threads them into `create_control_panel_in`.

**E1.7** (`crates/emcore/src/emPanelTree.rs:2080-2102`): The non-cross-tree
`CreateControlPanel` has the same latent bug (uses bare `PanelCtx::new`) but
is only called from unit tests (`emPanelTree.rs:3065, 3078`) that use a
trivial `ControlCreator` behavior which does not need scheduler reach. Not
currently triggered in production.

**E1.8** (`crates/emfileman/src/emDirEntryPanel.rs:616-626`): Same
`as_sched_ctx().expect(...)` pattern — would panic identically if reached via
the same path. Any fix must cover emDirEntryPanel too (not just emDirPanel).

### Root cause chain

1. User zooms into File System panel → content view's active panel changes →
   emView fires `control_panel_signal`.
2. `ControlPanelBridge::Cycle` (emMainWindow.rs:808) runs, walks to the active
   panel, and calls `content_sub_tree.create_control_panel_in(active, ctrl_sub_tree, ctrl_root, "context", tallness)`
   (emMainWindow.rs:855).
3. `create_control_panel_in` (emPanelTree.rs:2109) builds `PanelCtx::new(...)`
   without scheduler-reach handles (E1.4, E1.5).
4. The walk reaches `emDirPanel` as a parent-chain ancestor of the active panel.
   `emDirPanel::CreateControlPanel` calls `parent_ctx.as_sched_ctx().expect(...)`.
5. `as_sched_ctx` returns `None` because all five handles are `None` (E1.3) →
   `.expect` panics with the observed message.

### Phase 1 gate

Root cause located at `crates/emcore/src/emPanelTree.rs:2120` (the `PanelCtx::new`
inside `create_control_panel_in`). Evidence cited above; no fix proposed in
this phase.

## Phase 2 — Pattern Analysis

(empty — fill when phase 1 gate passes)

## Phase 3 — Hypotheses

(empty — fill when phase 2 gate passes)

## Root Cause

(empty — fill when phase 3 gate passes)

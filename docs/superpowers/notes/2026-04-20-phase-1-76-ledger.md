# Phase 1.76 — `PanelBehavior::Input` throwaway scheduler elimination — Ledger

**Started:** 2026-04-20
**Branch:** port-rewrite/phase-1-76
**Baseline:** see 2026-04-20-phase-1-76-baseline.md
**Spec sections:** §3.3 (Phase-1.76 Input throwaway callout — deleted in Task 3)
**JSON entries to close:** E039

## Task log

<!-- tasks append here as they complete -->

### Task 1 — Audit complete; `PanelCtx::with_scheduler` already suffices for input-dispatch use. Proceed to Task 2 signature change.

**Path taken:** Step 3 (no code changes).

Constructors found in `crates/emcore/src/emEngineCtx.rs` (lines 288–313):

```rust
// Constructor without scheduler — wakeup methods are no-ops.
pub fn new(
    tree: &'a mut PanelTree,
    id: PanelId,
    current_pixel_tallness: f64,
) -> Self

// Constructor with scheduler — wakeup methods propagate to the engine.
pub fn with_scheduler(
    tree: &'a mut PanelTree,
    id: PanelId,
    current_pixel_tallness: f64,
    scheduler: &'a mut EngineScheduler,
) -> Self
```

`scheduler` is taken as `&'a mut EngineScheduler` (not `Option<...>`); the `Option` wrapping is internal to the struct field. `PanelCtx::new` sets `scheduler: None`.

Task 2 will use `with_scheduler` at the input-dispatch call site (where a real scheduler is available) and `new` in test-mode contexts where no scheduler exists.

### Task 2 — KEYSTONE: widen `PanelBehavior::Input` with `&mut PanelCtx`; delete throwaway scheduler.

**Path taken:** Single atomic signature change, no intermediate `--no-verify` checkpoint needed.

**Changes:**
- `crates/emcore/src/emPanel.rs`: trait default `Input` gains `_ctx: &mut PanelCtx`.
- `crates/emcore/src/emSubViewPanel.rs`: `throwaway_sched_input` and its tombstone comment (lines 287–300, 301) deleted. Two `SchedCtx` constructions in `set_active_panel` and `sub_view.Update` call sites moved to separate scoped blocks, each re-borrowing `ctx.scheduler.as_deref_mut().expect(...)`. Recursive sub-tree dispatch constructs fresh per-sub-panel `PanelCtx` (tree=sub_tree, id=panel_id, pixel_tallness, re-borrowed scheduler via `match ctx.scheduler.as_deref_mut()`).
- `crates/emcore/src/emWindow.rs` (`dispatch_input`): constructs `PanelCtx::with_scheduler(tree, panel_id, pixel_tallness, ctx.scheduler)` per panel.
- `crates/emcore/src/emPanelTree.rs` (`dispatch_input`): builds a `PanelCtx::new(self, id, pixel_tallness)` (no scheduler — `emView::RecurseInput` caller lacks one; matches pre-1.76 observable behavior for that dead-code path).
- 19 production overrides updated with `_ctx: &mut PanelCtx` parameter (forwarding sites drop ctx at the boundary for out-of-scope inherent widget `.Input` helpers: emButton, emCheckBox, emCheckButton, emScalarField, emListBox, emRadioButton — unchanged per plan Known-but-descoped #1).
- Test files updated: `tests/support/mod.rs`, `tests/support/pipeline.rs` (construct ctx at dispatch site, threading `&mut self.scheduler`), `tests/golden/composition.rs` (9), `tests/golden/test_panel.rs` (10), `tests/golden/widget_interaction.rs` (1 override + 1 helper dispatch), `tests/pipeline/*.rs` (13 files). `emStocksFilePanel` test-module (17 `.Input(...)` call sites) given a `make_test_tree()` + `make_test_pctx(&mut tree, root)` helper.
- `emMainPanel` test-module (`SliderPanel` tests, 2 sites): added local `tree`/`root`/`PanelCtx::new(...)` construction.

**Diff scope:** 29 files, roughly +205/-66 lines per `git diff --cached --stat` pre-commit.

**`emColorField::Input` audit (mandated by plan):** Reviewed `crates/emcore/src/emColorField.rs:472–488`. Body only calls `hit_test` + `set_expanded` (which toggles `self.expanded` and invokes `auto_expand`/`auto_shrink` setters on local state). No signal firing, no engine wakeup, no scheduler interaction. Safe to leave out of scope. Result: **NO signals** — descoping confirmed correct.

**Observable behavior audit (C++ comparison):** `~/git/eaglemode-0.96.4/src/emCore/emSubViewPanel.cpp:69–79` — C++ `emSubViewPanel::Input` calls `Focus()`, `SubViewPort->SetViewFocused(...)`, `SubViewPort->InputToView(event,state)`, then `emPanel::Input`. All invocations route through the single shared scheduler (C++ `emEngine` has one scheduler per `emScheduler` instance, shared across the tree). All three audit conditions hold: (1) C++ invokes inline SetActivePanel + Update-equivalent via InputToView → View::Input, (2) wakes propagate on the shared scheduler, (3) no C++ guard suppresses signal propagation. Post-1.76 Rust wakes now reach the real outer scheduler, matching C++.

**Gate results:** fmt clean; clippy --all-targets --all-features -D warnings clean; nextest 2454 passed / 0 failed / 9 skipped; goldens 237 passed / 6 failed (identical inherited failure set: `composition_tktest_1x`, `composition_tktest_2x`, `notice_window_resize`, `testpanel_expanded`, `testpanel_root`, `widget_file_selection_box`). No goldens shifted — unmasked-wake hypothesis did not surface a latent bug.

**Invariants verified:**
- I-1.76-throwaway: `rg 'throwaway_sched_input' crates/emcore/src/emSubViewPanel.rs` → 1 match (comment documenting its removal — no production local).
- I-1.76-signature: `PanelBehavior::Input` trait default at `emPanel.rs:201–208` has `_ctx: &mut PanelCtx`.
- I-1.76-cascade: all production `.Input` call sites pass `&mut PanelCtx`; all 19 production overrides have the new param.
- I-1.76-no-new-hacks: `#[allow(...)]` count outside whitelist unchanged (22 vs 22 baseline).
- Test-module `EngineScheduler::new()` instances (e.g. `emSubViewPanel.rs:530` `outer_sched`) are inside `#[cfg(test)]` blocks — permitted per plan.

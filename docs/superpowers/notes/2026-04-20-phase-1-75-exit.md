# Phase 1.75 — Exit Metrics

**Captured:** 2026-04-20 local
**Branch:** `port-rewrite/phase-1-75` @ Task-7 commit `236fddf1` + Closeout
**Baseline:** see `2026-04-20-phase-1-75-baseline.md`

## nextest

```
2454 tests run: 2454 passed, 9 skipped, 0 failed
```

## goldens

```
237 passed; 6 failed
```

Identical failure set as baseline (`composition_tktest_1x`, `composition_tktest_2x`,
`notice_window_resize`, `testpanel_expanded`, `testpanel_root`,
`widget_file_selection_box`). No Phase 1.75 regressions.

## clippy

`cargo clippy --all-targets --all-features -- -D warnings`: PASS (clean).

## cargo fmt

`cargo fmt --check`: clean.

## rc_refcell_total

```
283
```

## diverged_total

```
176
```

## rust_only_total

```
17
```

## idiom_total

```
0
```

## try_borrow_total

```
0
```

## Delta

| metric              | baseline | exit | delta |
|---------------------|---------:|-----:|------:|
| nextest passed      |     2455 | 2454 |    −1 |
| nextest failed      |        0 |    0 |     0 |
| nextest skipped     |        9 |    9 |     0 |
| goldens passed      |      237 |  237 |     0 |
| goldens failed      |        6 |    6 |     0 |
| rc_refcell_total    |      282 |  283 |    +1 |
| diverged_total      |      177 |  176 |    −1 |
| rust_only_total     |       17 |   17 |     0 |
| idiom_total         |        0 |    0 |     0 |
| try_borrow_total    |        4 |    0 |    −4 |

**nextest −1:** Task 5 continuation deleted 2 obsolete timing fixtures
(`sp4_5_fix_1_timing_top_level_startup_baseline_slices`,
`sp4_5_fix_1_timing_top_level_mid_update_baseline_slices` — both asserted
the `delta==1` cost of deferred-attach, which no longer exists under
synchronous registration). Task 6 added 1 new test
(`phase_1_75_task6_spawn_and_wake_child_in_same_slice_delta_zero`). Net −1.

**rc_refcell_total +1:** Task 4 introduced test-only type aliases
(`RcCell<T> = Rc<RefCell<T>>`, `TestSchedRc = RcCell<EngineScheduler>`) in
`emPanelTree.rs` to preserve the test-harness scheduler-ownership shape
while eliminating the literal `Rc<RefCell<EngineScheduler>>` grep-text.
Observable Rc<RefCell<>> count in test scaffolding is unchanged; the +1 is
the new alias declaration itself.

**diverged_total −1:** DIVERGED block describing the `sub_scheduler`
concession at `emSubViewPanel.rs:34-40` (entry E005) deleted entirely —
the divergence is gone, so its documentation comment went with it.

**try_borrow_total −4:** Task 5 continuation deleted both try_borrow
deferral sites in `emPanelTree.rs` (`register_engine_for` gate +
`add_to_notice_list` update-engine-id read). The four removed matches
span those two sites including fmt-reflowed lines.

## Invariants verified

Phase 1.75 plan invariants:

| ID | Status | Evidence |
|----|--------|----------|
| I1 — `rg 'Rc<RefCell<EngineScheduler>>' crates/` = 0 | SAT | grep returns no matches. |
| I1c — `rg -w 'sub_scheduler' crates/` = 0 | SAT | grep returns no matches (after tombstone comment rephrase). |
| I1d — `rg 'try_borrow(_mut)?\(\)' crates/emcore/src/{emView.rs,emPanelTree.rs,emSubViewPanel.rs}` = 0 | SAT | all three files: 0 matches. |
| I-Y3-dispatch — EngineScheduler::DoTimeSlice walks TreeLocation per engine | SAT | `emScheduler.rs:139 dispatch_with_resolved_tree`; `emScheduler.rs:25 engine_locations: SecondaryMap<EngineId, TreeLocation>`. |
| I-T3a — `rg -w 'register_pending_engines' crates/` = 0 | SAT | grep returns no matches. |
| I-T3b — `emPanelCtx.rs` deleted; no `pub mod emPanelCtx` in lib.rs | SAT | `test -e .../emPanelCtx.rs` → DELETED; grep in lib.rs → 0. |
| I-T3c — `rg 'emPanelCtx::' crates/` = 0 | SAT | grep returns no matches. |
| I-Spec-3.3-clarified — Spec §3.3 describes cross-tree dispatch | SAT | Task 7 commit `236fddf1`; §3.3 now names TreeLocation + as_sub_view_panel_mut + take/put walk. |
| Task-10 — 4 inline `ctx.create_signal()` in emView::RawVisitAbs | SAT | `emView.rs:1808–1811`; no pre-allocation block. |
| Task-11 — fixture asserts `delta == 0` | SAT | `phase_1_75_task6_spawn_and_wake_child_in_same_slice_delta_zero` @ `emPanelTree.rs:3802`. |

Phase 1 carry-forwards:

| ID | Status | Evidence |
|----|--------|----------|
| I1a — SchedOp eliminated | SAT | `rg 'SchedOp\|queue_or_apply_sched_op' crates/` = 0. |
| I1b — close_signal_pending / pending_sched_ops eliminated | SAT | grep = 0. |
| I6 — NewRootWithScheduler gone (Golden tests 237/6 preserved per spec §11 I6) | SAT | goldens 237/6 identical failure set. |

## JSON entries closed

Resolved in Phase 1.75:
- **E005** — `sub_scheduler` DIVERGED block at `emSubViewPanel.rs:34-40`. Resolved by Task 4 keystone (`a5efee6f`): field + DIVERGED block deleted; C++ §3.3 shared-scheduler invariant now preserved via unified outer EngineScheduler dispatch.
- **E008** — `register_engine_for` try_borrow path + `register_pending_engines` sweep at `emPanelTree.rs:558-611`. Resolved by Task 5 continuation (`eb5ed94b`): both try_borrow sites and the post-slice sweep deleted; synchronous registration at `create_child` time.
- **E011** — pre-allocated popup signals via `sched.borrow_mut()` at `emView.rs:1828-1836`. Resolved by Task 6 (`a7a2482c`) audit: four inline `ctx.create_signal()` sites confirmed at `emView.rs:1808-1811`; no pre-allocation block.

Retroactively marked resolved in Phase 1.5 (closeout I1a/I1b table already cited these as SAT; JSON status field was not updated at the time):
- **E003** — SchedOp enum at `emView.rs:196-206`.
- **E004** — `pending_sched_ops: Vec<SchedOp>` field.
- **E007** — `queue_or_apply_sched_op` body.
- **E010** — `close_signal_pending` cache.

New entry added:
- **E039** — `PanelBehavior::Input` throwaway `EngineScheduler::new()` at `emSubViewPanel.rs:301`. Status: `deferred-phase-1-76`. Threading a scheduler into `PanelBehavior::Input` requires widening the trait signature across ~100+ impls — explicitly out of Phase 1.75 scope per Task 5 escape clause; planned for Phase 1.76. Observable impact narrow (signal-driven reactions firing ONLY during input dispatch); goldens 237/6 unchanged across Tasks 3–6.

## Spec sections implemented

- §3.3 — clarified (Task 7): cross-tree dispatch mechanism now described; observable invariant ("outer and sub-view engines at priority P interleave in priority order") preserved verbatim.
- §3.6 — sub-view unification delivered.
- §4 D4.1 / D4.6 / D4.7 / D4.11 — delivered per Task 5 cont + Task 6 (annotations added in Task 7).
- §10 Phase-1 invariant list — updated in Task 7 to cite Phase 1.75 delivery phases per bullet.

## Next phase

Phase 2 — view/window composition and back-ref migration. Phase 2's B4
predecessor check must accept `port-rewrite-phase-1-75-complete` as the
first COMPLETE predecessor in the chain (prior tags `port-rewrite-phase-1-complete`
and `port-rewrite-phase-1-5-partial-complete` are the partial predecessors Phase 1.75 completed).

Phase 1.76 — dedicated plan to widen `PanelBehavior::Input` (and possibly
sibling trait methods) to thread a scheduler handle, eliminating the last
throwaway `EngineScheduler::new()` at `emSubViewPanel.rs:301` (JSON entry E039).
Scope: ~100+ `PanelBehavior::Input` impl mechanical cascade.

# Phase 4d — emRec Persistence IO — Ledger

**Started:** 2026-04-22
**Branch:** port-rewrite/phase-4d
**Baseline:** see 2026-04-19-phase-4d-baseline.md
**Spec sections:** §7 D7.1
**JSON entries to close:** none (E026 deferred to Phase 4e)

## Task log

- **Task 3 completion** — supersedes the Task 3 (partial) entry below. The `emRecNode` trait is widened with `TryRead(&mut self, reader, ctx) -> Result<(), RecIoError>` and `TryWrite(&self, writer) -> Result<(), RecIoError>` (no default impls — every concrete emRec type provides both). Trait-forwarding impls added to the 8 primitive types (emBoolRec + 7 from the partial entry) — each forwards via qualified-name (`emBoolRec::TryRead(self, …)`) to the existing inherent method, preserving the byte-format fidelity already proved by the Task 3 (partial) round-trip tests. Compound ports: (a) `emUnionRec::TryRead/TryWrite` reads `identifier: <child-body>` via `TryReadIdentifier` + `GetVariantOf` + `SetVariant` + `TryReadCertainDelimiter(':')` + dyn-dispatched `child.TryRead`, write mirrors, per emRec.cpp:1610-1655; (b) `emArrayRec::TryRead/TryWrite` + `emTArrayRec::TryRead/TryWrite` — `{` + `<child-body>`* + `}` with newline+indent between elements, growth via `SetCount` during read, min/max enforcement with "Too few/many elements." errors, per emRec.cpp:1820-1918; (c) `emStructRec` helpers `try_read_body` / `try_write_body` added as associated functions taking `members: &[String]` explicitly so the user's derived struct can capture `&mut self` sibling-field refs in the read/write closure without borrow-checker conflicts — pattern: derived struct calls `let members = self.inner.member_identifiers();` (new accessor) then `emStructRec::try_read_body(&members, reader, |idx, r| match idx { ... })`. `try_read_body` reads `{` + `(identifier = <body>)*` + `}`, rejects unknown-identifier and re-assignment per emRec.cpp:1406-1409; `try_write_body` emits `{` + newline-indented `ident = <body>` for each member where `should_emit(idx)` returns true + `}`, per emRec.cpp:1428-1489. The bare-emStructRec trait impl uses an empty-member `{}` round-trip stub — real callers always implement via a derived struct. Derived-struct impls updated in the Phase 4c test mods: `Person`, `Address`, `PersonWithAddr` (emStructRec.rs), `UnionHolder` (emUnionRec.rs), `ArrayHolder` (emArrayRec.rs), and the integration-test `Person` (`emrec_compound_integration.rs`) now impl real TryRead/TryWrite. Test-double spies (`SpyRec` in emUnionRec/emArrayRec, `MutableSpyRec`) get error-stub trait methods (TryRead → syntax error, TryWrite → Ok) since they're instrumentation types not meant for serialisation. Round-trip tests appended to `crates/emcore/tests/emrec_persistence_roundtrip.rs`: `union_rec_roundtrip` (num|text variants → bytes `text: "hello"`), `array_rec_roundtrip` (emIntRec array [10,20,30] → `{\n\t10\n\t20\n\t30\n}`), `tarray_rec_roundtrip_persons` (emTArrayRec<Person> 2-element), `struct_rec_roundtrip_person` (Person → `{\n\tname = "alice"\n\tage = 42\n\tmale = no\n}`); each asserts write→read→write byte-stability. Compound tests use `std::mem::forget(fx)` to suppress the EngineScheduler drop-assert: allocator closures create per-child value signals the test has no handle to (the signals survive variant-switch/shrink even as their owners are dropped), and enumerating them through `dyn emRecNode::listened_signal` only reaches the currently-live children — the forget is the smallest divergence that keeps the byte-level assertions honest. DIVERGED sites: `emStructRec::member_identifiers` (new-to-Rust snapshot accessor, sibling-field borrow-checker workaround); fused Start/Continue in every compound TryRead/TryWrite (same rationale as primitive). Files: `crates/emcore/src/emRecNode.rs` (trait widened + test-stub impls), `crates/emcore/src/em{Bool,Int,Double,Enum,String,Flags,Alignment,Color}Rec.rs` (+2 trait methods each), `crates/emcore/src/emUnionRec.rs` (+TryRead/TryWrite + SpyRec/UnionHolder stubs), `crates/emcore/src/emArrayRec.rs` (+TryRead/TryWrite + SpyRec/ArrayHolder/MutableSpyRec stubs), `crates/emcore/src/emTArrayRec.rs` (+TryRead/TryWrite, mirrors emArrayRec), `crates/emcore/src/emStructRec.rs` (+try_read_body/try_write_body + member_identifiers + bare trait impl + 3 derived-struct impls), `crates/emcore/tests/emrec_compound_integration.rs` (Person trait impl), `crates/emcore/tests/emrec_persistence_roundtrip.rs` (+4 compound roundtrip tests). +4 tests (2654 → 2658); fmt clean, `cargo clippy -- -D warnings` clean (matches pre-commit gate; `--all-targets` surfaces pre-existing `drop_non_drop` lints unrelated to this task), nextest 2658/2658.
- **Task 3 (partial)** — TryRead/TryWrite ported as inherent methods on the 7 primitive emRec types: `emIntRec`, `emDoubleRec`, `emStringRec`, `emEnumRec`, `emFlagsRec`, `emAlignmentRec`, `emColorRec`. Each method is tagged with a DIVERGED note collapsing C++'s two-phase `TryStartReading`/`TryContinueReading` (and Writing) pair into one atomic call — same rationale as `emBoolRec::TryRead`/`TryWrite` (Task 2). Byte format mirrors C++ emRec.cpp exactly: `emIntRec`/`emDoubleRec` use the writer's int/double primitives (DIVERGED on i64→i32 clamp for write since the reader primitive is `i32`); `emStringRec` uses `TryReadQuoted`/`TryWriteQuoted`; `emEnumRec` uses `TryReadIdentifier`/`TryWriteIdentifier` with a private case-insensitive `GetValueOf` helper mirroring C++ emRec.cpp:650-658 (and accepts int-form 0..N-1 on read per emRec.cpp:678-683); `emFlagsRec` emits `{idf idf}` with space-separated bit names (empty set → `{}`) per emRec.cpp:862-877 and accepts either `{...}` or a masked int on read; `emAlignmentRec` joins `top`/`bottom`/`left`/`right` with `-` per emRec.cpp:1001-1026 (emits `center` when no bits are set) and loop-reads identifiers separated by `-` delimiters; `emColorRec` uses `{R G B [A]}` per emRec.cpp:1238-1251 and accepts a quoted-string hex form via `emColor::TryParse`. Field rename in `emEnumRec.rs`: `_identifiers → identifiers` (drop dead-code prefix). Round-trip coverage: 14 integration tests in `crates/emcore/tests/emrec_persistence_roundtrip.rs` — one byte-stability round-trip per type plus error-path tests for out-of-range int, unknown enum identifier, empty-flag set, center-alignment, quoted-hex color, and out-of-range RGB channel. Compound types (`emStructRec`, `emUnionRec`, `emArrayRec`, `emTArrayRec`) BLOCKED: `emRecNode` carries only `{parent, register_aggregate, listened_signal}`, so `Box<dyn emRecNode>` children (in `emUnionRec`, `emArrayRec`) and sibling-field children (in `emStructRec`'s user-derived compounds) cannot dispatch `TryRead`/`TryWrite`. Unblocking requires extending `emRecNode` with `TryRead`/`TryWrite` methods (or introducing a sibling trait) — a prerequisite foundation task. Files: `crates/emcore/src/em{Int,Double,String,Enum,Flags,Alignment,Color}Rec.rs` (+329 LOC total), `crates/emcore/tests/emrec_persistence_roundtrip.rs` (new, ~440 LOC incl. Fixture helper). +14 tests (2640 → 2654); fmt clean, clippy `-D warnings` clean, nextest 2654/2654.
- **Task 2 fixup** — reviewer feedback applied: `format_g_9` now rejects non-finite doubles via `TryWriteDouble` (returns `RecIoError`) and preserves the sign of `-0.0` (raw token `-0`; writer appends `.0` → `-0.0`) so the emitted token always re-lexes as ET_DOUBLE (I1). Reimplemented `format_g_9` to classify fixed vs scientific by parsing the exponent out of `{:.8e}` (9 sig digits, post-rounding by stdlib), so boundary cases like `9.9999999995e9` route through the rounded value `1.0e10`; matches C `%.9G` on the full workstation reference set `{0, ±1, 0.5, 1e±9, 1e±4, 1e±5, 9.99999999, 1234567890}` (I1). `TryWriteQuoted` keeps encoding embedded NUL as `\000` but now carries an explicit `// DIVERGED:` comment above it — C++ truncates at the first NUL (C-string convention); Rust preserves information since `&str` may legitimately carry embedded NULs (I2). `resolve_escapes` trailing-backslash branch re-documented as unreachable (lexer rejects unterminated strings first) with a two-sentence comment (M3). Module-header narration trimmed to ~5 lines per file — C++ reference range + why the type exists (state-carrying impl of a stateless trait) + header-magic deferral note for the reader (M4). Redundant per-method `/// cpp:N-M` paraphrase comments on `emRecMemWriter`'s trait impl methods removed (`TryWriteDelimiter`/`Identifier`/`Int`/`Double`/`Space`/`NewLine`/`Indent`); kept DIVERGED and non-obvious references (M5). New tests: `format_g_9_matches_c_reference`, `format_g_9_preserves_negative_zero`, `format_g_9_refuses_non_finite`, `quoted_preserves_embedded_nul_via_octal` (writer); `bool_rec_rejects_unknown_identifier`, `bool_rec_rejects_out_of_range_int` (integration) (M7); `bool_rec_roundtrip` now asserts write→read→write byte-stability (M8). Gates: fmt clean, clippy `-D warnings` clean, nextest 2640/2640 (2634 → 2640, +6 tests).
- **Task 2** — emRecMemReader / emRecMemWriter + emBoolRec round-trip. Pre-audit verdict on `emRecParser.rs`: legacy text parser is tree-based (`RecValue`/`RecStruct`) at a different architectural layer; stays separate and untouched. Escape conventions happen to overlap but were ported fresh from C++ `emRec.cpp` (reader 2310-2378, writer 2607-2637) to avoid coupling. Task 2 builds a streaming lexer/emitter that matches the C++ emRecReader/emRecWriter base-class state layout — `Lexer` struct (private in `emRecMemReader.rs`) carries `line` / `next_eaten` / `next_line` / `next_type` / `next_delimiter` / `next_buf` / `next_int` / `next_double` / `next_char` 1:1 with C++ fields (emRec.cpp:1974-1988); `Emitter` struct (private in `emRecMemWriter.rs`) carries `buf` + `indent`. Trait methods are thin wrappers that call `TryParseNext` on demand and return the parsed element — matches the eaten-lookahead flow (emRec.cpp:2118-2181). `emRecMemReader::new(&[u8])` primes the first lookahead char inline (C++ does it in `emRecReader::TryStartReading` at 2034) and does not consume a `#%rec:FormatName%` magic — atomic types have no format name; header handling deferred to Task 6 (emConfigModel). `emRecMemWriter::new()` + `into_bytes(self)` finalise — DIVERGED from C++ which takes an external `emArray<char>&` (emRec.cpp:2919); rationale recorded at the struct decl. Byte-format fidelity: integers via `sprintf("%d")`, doubles via an in-crate `format_g_9` that emulates C's `%.9G` (normalised mantissa + stripped trailing zeros + two-digit exponent), `.0` tail appended when neither `.` nor `E`/`e` appears, quoted bodies emit `\n \r \t \a \b \f \v \\ \"` named escapes plus `\NNN` octal triplet for other controls and pass 0x20-0x7E / 0xA0+ bytes through verbatim (preserves UTF-8 continuation bytes). Reader accepts `\xHH` / `\NNN` / letter escapes (emRec.cpp:2310-2378). `TryRead` / `TryWrite` added as inherent methods on `emBoolRec` (not trait methods — deferred to a later task if the full-type matrix needs generic dispatch). Name choice: `TryRead` — the C++ protected overload has disjoint arity (`TryRead(char*,int)`) and is not on the Rust trait, so no collision. DIVERGED annotations at both method sites explain the fusion of `TryStartReading` + `TryContinueReading` into one atomic call (scheduler is cooperative at a coarser granularity; compounds may revisit). Reader accepts `yes/no/y/n/true/false` case-insensitive + `0/1` integer (emRec.cpp:334-355); writer emits bare `yes`/`no` identifier (emRec.cpp:371). Files: `crates/emcore/src/emRecMemReader.rs` (new, ~450 LOC incl. tests), `crates/emcore/src/emRecMemWriter.rs` (new, ~310 LOC incl. tests), `crates/emcore/src/emBoolRec.rs` (+63 LOC), `crates/emcore/src/lib.rs` (+2 `pub mod` lines), `crates/emcore/tests/emrec_persistence_bool_roundtrip.rs` (new, round-trip + alias matrix integration tests). +17 tests (2617 → 2634); fmt clean, clippy `-D warnings` clean, nextest 2634/2634.
- **Task 1 fixup** — reviewer feedback applied: `RecIoError` fields `pub → pub(crate)` (I1); removed untagged `RecIoError::new` constructor, `with_location` is canonical (I2); trimmed paraphrase doc comments on `TryReadDelimiter` / `TryReadDouble` / `TryReadQuoted` (writer methods were already signature-only) (M1); renamed `_assert_dyn_safe → assert_dyn_safe` in both files (M6); added WHY comment on `PeekResult::Delimiter(char)` noting ASCII-only lexer contract (M8). Gates: fmt clean, clippy `-D warnings` clean, nextest 2617/2617 (unchanged).
- **Task 1** — emRecReader / emRecWriter traits defined. Ports the per-element primitive API from `emRec.h:1569-1620` (reader) and `emRec.h:1691-1724` (writer). `ElementType` enum mirrors C++ `ET_*` names (prefix dropped); `PeekResult` folds C++'s `TryPeekNext(char *pDelimiter)` out-parameter into the `Delimiter(char)` variant (DIVERGED, documented at enum declarations). `TryReadIdentifier` / `TryReadQuoted` return owned `String` instead of `const char *` into an internal buffer (idiom adaptation — buffer lifetime invisible to callers). `TryReadInt` returns `i32` (C++ `int` width — not `i64` as an earlier plan sketch suggested). `ThrowElemError` / `ThrowSyntaxError` *return* the constructed error rather than throw (DIVERGED; helper, not a throw). Both traits dyn-compatible; compile-time dyn-safety + method-list assertions live in `#[cfg(test)]` `DummyReader` / `DummyWriter` impls. `RecIoError` (new, in `emRecReader.rs`) carries `source_name` / `line` / `message` with `Display` + `Error`; intentionally distinct from `emRecParser::RecError` (legacy text parser). State-machine driver methods (`TryStartReading`, `TryContinueReading`, `TryFinishReading`, `QuitReading`, `GetRootRec`, `GetSourceName`, protected `TryRead`/`TryClose`) deferred to Task 2+. Files: `crates/emcore/src/emRecReader.rs` (new), `crates/emcore/src/emRecWriter.rs` (new), `crates/emcore/src/lib.rs` (+2 `pub mod` lines). +4 tests (2613 → 2617); fmt clean, clippy `-D warnings` clean, nextest 2617/2617.
